# ---------------------------------------------------------------------------
# Rust / Cargo
# ---------------------------------------------------------------------------
# Workspace build artifacts (single shared target dir at the repo root).
/target/

# Cargo.lock IS committed on purpose: this workspace ships applications
# (frontend WASM, `server`, `resume-generator`), so a locked dependency
# graph is required for reproducible builds and `cargo audit`.
# Do NOT add Cargo.lock here.

# rustfmt backups and debug symbols
**/*.rs.bk
*.pdb

# ---------------------------------------------------------------------------
# Trunk / WASM frontend
# ---------------------------------------------------------------------------
# Trunk build output (apps/frontend/dist) — also holds the generated
# resume PDFs and resume-fingerprint.json from the resume-generator.
dist/

# Note: apps/frontend/repos.json is generated AND committed by the
# `update-repos` workflow, so it is intentionally tracked (not ignored).

# ---------------------------------------------------------------------------
# Node (Tailwind CSS tooling only)
# ---------------------------------------------------------------------------
node_modules/

# The build uses `npm install`, not `npm ci`, so lockfiles are not committed.
package-lock.json
yarn.lock
pnpm-lock.yaml
bun.lockb
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ---------------------------------------------------------------------------
# Environment & secrets
# ---------------------------------------------------------------------------
.env
.env.local
.env.*.local
*.pem
*.key

# ---------------------------------------------------------------------------
# Local agent / tooling state
# ---------------------------------------------------------------------------
.claude/
.serena/

# ---------------------------------------------------------------------------
# IDE / editor
# ---------------------------------------------------------------------------
.vscode/
.idea/
*.iml
*.swp
*.swo

# ---------------------------------------------------------------------------
# OS noise
# ---------------------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini

# ---------------------------------------------------------------------------
# Logs
# ---------------------------------------------------------------------------
*.log
